flowchart LR
    Scout["SSA ExplorerScout<br/>(app pack)"] --> Queries["ExplorationQuery[]"]

    subgraph SWARM["NanoSwarm<br/>runtime-configured waves"]
        direction TB
        Profile["NanoSwarmProfile<br/>lenses + prompts"]
        Decompose["decompose to micro-queries"]
        Waves["wave executor<br/>waveSize / delay / maxMicroQueries"]
        Merge["URL dedup + entity extraction"]
    end

    subgraph APP["SSA explorer collapse<br/>(app pack)"]
        direction TB
        Curator["ExplorerCurator<br/>LLM or heuristic fallback"]
        SourceOps["source_item injection<br/>optional source promotion"]
    end

    Queries --> Decompose
    Profile --> Decompose
    Decompose --> Waves
    Waves --> Merge
    Merge -->|"NanoArticle[] + stats"| Curator
    Curator --> SourceOps
