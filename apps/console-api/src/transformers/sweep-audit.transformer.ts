// Shared types for sweep_audit writes live in types/sweep.types.ts.
// This transformer file is intentionally empty (no mapping helpers yet);
// keep it as a place to add future row→DTO mappers for sweep_audit.
export {};
